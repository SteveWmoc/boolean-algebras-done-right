import BooleanAlgebrasDoneRight.Order

/-!
# Mathlib interoperability

This module is the boundary between the algebra-first development in this
repository and Mathlib's lattice-first Boolean-algebra hierarchy.

The intended direction is from the intrinsic Boolean theory to derived
order/lattice structures and then, where practical, to Mathlib-compatible
instances and equivalences.  The bridge should not determine the foundational
ontology of this project.

Global instances are to be introduced cautiously to avoid typeclass diamonds.
-/

namespace BooleanAlgebrasDoneRight

end BooleanAlgebrasDoneRight
